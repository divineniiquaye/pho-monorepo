{
  "name": "pho-monorepo",
  "version": "0.2.20",
  "pho": "0.2.20",
  "bin": {
    "pho-monorepo": "scripts/init.mjs"
  },
  "files": [
    "scripts/init.mjs",
    "CHANGELOG.md"
  ],
  "packageManager": "pnpm@10.15.0",
  "workspaces": [],
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:ios": "node scripts/build.mjs --platform ios",
    "build:android": "node scripts/build.mjs --platform android",
    "dev": "node scripts/run.mjs dev",
    "test": "node scripts/run.mjs test",
    "deps": "node scripts/deps.mjs",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "docker": "docker-compose -f docker-compose.yml up",
    "watchman": "watchman watch-del './' ; watchman watch-project './'",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\" --ignore-path .gitignore",
    "locale": "pnpm run --filter=@tooling/i18n locale"
  },
  "nohoist": [
    "**/expo-router",
    "**/expo-router/**"
  ],
  "dependencies": {
    "@inquirer/select": "^4.3.1",
    "@clack/prompts": "^0.11.0",
    "chalk": "^5.6.0",
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "cross-env": "10.0.0",
    "prettier": "^3.6.2",
    "turbo": "^2.5.6"
  },
  "engines": {
    "node": ">=20"
  },
  "i18n-config": {
    "paths": [
      "./apps/web",
      "./apps/native"
    ],
    "include": [
      "./packages/design-system/components"
    ]
  }
}
